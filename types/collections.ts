export interface ICollectionMeta {
	phrasesCount: number,
	repetitionsCount: number,
	lastRepetition: number | null
}

export interface IAutoGeneratedCollectionMeta {
	type: "auto" | "htm" | "interval"
}

export interface ICollection {
	id: string,
	name: string,
	isLocked: boolean,
	isAutoGenerated: boolean,
	color: string,
	created: number,
	lastUpdate: number,
	profile: string,
	meta: ICollectionMeta,
	repetitions: string[],
	userId: string
}

export interface IAutoCollection extends ICollection {
	phrases: string[],
	autoGeneratedCollectionMeta: IAutoGeneratedCollectionMeta
}

export type TCollectionNameId = Pick<ICollection, "name" | "id">